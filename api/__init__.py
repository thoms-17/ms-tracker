"""Back FastAPI de TV Time Analytics (mono-utilisateur, local).

Posé sur le cœur métier (`src/db`, `src/enrich`, `src/stats`, `src/models`) sans le modifier.
Lancer :  uvicorn api.main:app --reload
"""
